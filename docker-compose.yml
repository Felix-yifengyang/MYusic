services:
  app:
    build:
      context: .
    restart: unless-stopped
    env_file:
      - .env.docker
    ports:
      - "8787:8787"
    volumes:
      - app-data:/data/MYusic
      - music-library:/data/MYusic/library
    depends_on:
      postgres:
        condition: service_healthy
      navidrome:
        condition: service_started

  navidrome:
    image: deluan/navidrome:latest
    restart: unless-stopped
    user: "0:0"
    environment:
      ND_ADDRESS: 0.0.0.0
      ND_PORT: 4533
      ND_MUSICFOLDER: /music
      ND_DATAFOLDER: /data
    ports:
      - "4533:4533"
    volumes:
      - music-library:/music
      - navidrome-data:/data

  postgres:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_DB: myusic
      POSTGRES_USER: myusic
      POSTGRES_PASSWORD: your_password
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myusic -d myusic"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  app-data:
  music-library:
  navidrome-data:
  postgres-data:

