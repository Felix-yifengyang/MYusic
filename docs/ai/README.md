# AI 文档入口

这个目录只放给 AI / 开发代理读取的 Markdown 文档。

面向用户直接阅读的 HTML 文档放在：

```text
docs/user/
```

规则：

- `docs/ai/*.md`：保留工程背景、架构说明、实现约束、后续验收事项。
- `docs/user/*.html`：保留给项目使用者直接打开阅读的说明，优先精简合并。
- 不在 `docs/` 根目录继续放散乱文档。
