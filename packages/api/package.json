{
  "name": "@myusic/api",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "migrate:json-to-postgres": "pnpm build && node dist/scripts/migrate-json-to-postgres.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@myusic/downloader": "workspace:*",
    "@myusic/shared": "workspace:*",
    "fastify": "^5.3.3",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/pg": "^8.20.0"
  }
}
