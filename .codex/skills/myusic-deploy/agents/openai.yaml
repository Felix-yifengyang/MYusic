interface:
  display_name: "MYusic Deploy"
  short_description: "Enter the MYusic server and deploy the app"
  default_prompt: "Use the MYusic deploy skill to deploy the latest master branch to the production server."
